/** Z-index centralisés. */
export const zIndex = {
  base: 0,
  card: 10,
  badge: 20,
  header: 30,
  bottomNav: 40,
  sheet: 50,
  toast: 60,
} as const;

/** Opacités standard. */
export const opacity = {
  disabled: 0.4,
  pressed: 0.7,
  scrim: 0.42,
} as const;
